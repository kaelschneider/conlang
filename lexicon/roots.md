# Historical Root Inventory

## Status

This file records established roots and their known developments. Meanings are lexical associations, not productive phonological rules.

| Proto-root | Core meaning | Modern / derived evidence | Status |
|---|---|---|---|
| `*p` | break / bite | `a-pa > apa` 'eat'; word-initial `*p > h` | ESTABLISHED |
| `*t` | transfer | `ita` 'give'; `ata > ada > da` 'take'; `teta` 'put/set' | ESTABLISHED |
| `*k` | hold | `ka` 'hold'; `keka` 'keep' | ESTABLISHED |
| `*w` | grow | `wa` 'grow' | ESTABLISHED |
| `*r` | go | `ra` 'go'; `rera` 'turn' | ESTABLISHED |
| `*j` | see / know | `ya` 'see/know' | ESTABLISHED |
| `*h` | say / blow | `ha` 'say/blow'; source of exclamative `-h-` | ESTABLISHED |
| `*s` | cut / remove | `sa` 'cut/remove' | ESTABLISHED |
| `*m` | live / dwell / stay | `ma` 'live/dwell/stay' | ESTABLISHED |
| `*n` | get / find | `na` 'get/find'; `nena` 'receive/acquire' | ESTABLISHED |

## Root formation

Roots are historically monoconsonantal. Surface lexical stems are formed through vowel grades and historical derivation; apparent multi-consonant forms should not automatically be treated as independent lexical roots.

## Sound symbolism

The root inventory uses sound-symbolic semantic associations. These are lexical/historical tendencies rather than synchronically productive phonological rules. The full system remains ?.
